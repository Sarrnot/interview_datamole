# Work log

## Task

Add an arbitrary feature at the complexity level of: todo categories with filtering, repeating todos, drag-and-drop reordering. Implementation should be done across all layers frontend/backend/persistence. Majority of code should be generated by LLMs.

Compared to the original task, the evaluation will focus mainly on the process of using LLMs. The resulting code and application will also be evaluated, but rather from the perspective of how you achieved a high-quality generated output.

## Selected task

I chose to implement the drag-and-drop reordering.

## Log
