{
    "$schema": "https://json.schemastore.org/claude-code-settings.json",
    "permissions": {
        "allow": [
            "Bash(pnpm --filter client lint)",
            "Bash(pnpm --filter client exec tsc --noEmit)",
            "Bash(pnpm --filter client format)"
        ],
        "deny": []
    }
}
